# 书签导航扩展 (Bookmark Navigation Extension)

本项目是一个 Chrome 浏览器扩展，旨在将默认的“新标签页”替换为一个功能丰富的书签导航仪表板。它通过左侧目录树和右侧卡片网格的形式展示您的书签，让您更便捷地浏览和访问收藏的网站。

**最新更新 (2025-12-06):**
*   **侧边栏文件夹拖拽排序与嵌套**：实现了侧边栏文件夹的自由拖拽排序。支持将文件夹插入到其他文件夹的前面/后面，或者拖入其他文件夹内部（嵌套）。
*   **交互视觉优化**：文件夹拖拽时提供清晰的插入位置指示（上方/下方蓝线）和嵌套归档反馈（高亮背景）。
*   **深度交互优化**：实现了侧边栏文件夹的右键管理（新建/重命名/删除）、折叠状态记忆以及平滑的拖拽归档功能。
*   **视觉反馈升级**：拖拽排序采用无抖动的 CSS 指示线方案；拖拽归档采用清晰的边框高亮。
*   **安全性增强**：完全移除内联事件处理，严格遵守 CSP 安全策略。

## 项目概览

*   **名称:** 书签导航网站
*   **类型:** Chrome 扩展 (Manifest V3)
*   **核心功能:** 覆盖 `newtab` 页面以展示用户书签。
*   **权限:** `bookmarks` (读写权限，用于管理书签), `favicon` (用于获取网站图标)。

## 功能特性 (Key Features)

### 1. 侧边栏 (Sidebar)
*   **可折叠目录树**: 支持点击文件夹名称或箭头展开/折叠子目录。
*   **状态持久化**: 自动保存文件夹的展开状态 (`localStorage`)。
*   **文件夹拖拽排序与嵌套**:
    *   **排序**: 支持拖拽文件夹调整顺序（插入前/后）。
    *   **嵌套**: 支持将文件夹拖入另一个文件夹内部。
    *   **安全保护**: 自动防止将文件夹拖入其自身的子目录中。
*   **右键菜单**: 支持对文件夹进行 **新建子文件夹**、**重命名** 和 **删除** 操作。
*   **拖拽归档**: 支持将右侧书签拖入左侧文件夹，带有清晰的 `ring` 边框高亮反馈。

### 2. 主视图 (Main View)
*   **卡片网格**: 响应式展示书签，支持 Favicon 显示。
*   **拖拽排序**: 
    *   支持在当前文件夹内自由拖拽排序。
    *   **无抖动设计**: 使用 CSS `box-shadow` 绘制蓝色指示线，精准指示插入位置（前/后）。
*   **右键菜单**: 支持对书签进行 **编辑** (修改标题/URL) 和 **删除** 操作。
*   **面包屑导航**: 顶部显示当前路径，点击可快速返回上级。

### 3. 搜索 (Search)
*   **实时搜索**: 顶部输入框实时过滤所有书签。
*   **上下文提示**: 搜索结果卡片右下角显示该书签所属的 **父文件夹名称**，方便定位。

## 项目结构

*   `manifest.json`: 扩展配置文件 (V3)，定义权限和入口。
*   `newtab.html`: 主界面 HTML 结构。
*   `style.css`: 由 Tailwind CLI 编译生成的最终 CSS。
*   `script.js`: 核心业务逻辑。
    *   书签树遍历与渲染 (`renderBookmarkTree`)。
    *   书签网格渲染与拖拽逻辑 (`renderBookmarkSites`)。
    *   右键菜单与模态框逻辑。
    *   搜索功能实现。
*   `src/input.css`: Tailwind CSS 源文件。
*   `tailwind.config.js`: Tailwind 配置文件。

## 开发指南

### 1. 环境准备
确保安装 Node.js，首次运行：
```bash
npm install
```

### 2. 开发模式
启动 Tailwind 监听，实时编译 CSS：
```bash
npm run dev
```

### 3. 生产构建
```bash
npm run build
```

### 4. 安装体验
在 Chrome 浏览器访问 `chrome://extensions/`，开启开发者模式，加载本项目目录。